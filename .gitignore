# Dev SSH keypair — generated locally, never committed (see examples/docker/README.md)
examples/docker/sunwell_dev_key
examples/docker/sunwell_dev_key.pub

# Build output
target/
dependency-reduced-pom.xml

# Profiling and benchmark results (always local, never committed)
results/

# Claude Code local settings (user-specific, not project config)
.claude/settings.local.json

# IDE
.idea/
*.iml
.classpath
.project
.settings/
